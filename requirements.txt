# 共通
python-dotenv>=1.0
pyyaml>=6.0

# ブラウザ駆動エンジン（どちらか / 両方）
selenium>=4.20          # 既定エンジン（--engine selenium）
playwright>=1.40        # 任意エンジン（--engine playwright）。channel=msedge で導入済みEdgeを使用

# クラウド版バックエンド (agent.py) — Anthropic API を使う場合
anthropic>=0.40

# ローカル版バックエンド (agent_ollama.py) — Ollama を使う場合（API キー不要）
ollama>=0.3

# MCP サーバー版 (mcp_server.py)
mcp>=1.2
